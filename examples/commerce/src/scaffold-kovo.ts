import { defineKovo } from '@kovojs/server';

/**
 * The copied commerce scaffold is a production-buildable contract. The repository's full PGlite
 * and local-auth demo remains a non-shipped architecture reference until an app selects reviewed
 * deployment providers.
 */
export const app = defineKovo({
  appId: '24fa3049-67cd-4750-b6a7-c24ef5274324',
  document: { lang: 'en-US' },
});
