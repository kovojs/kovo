{
  "filePath": "packages/server/src/app-mutation-request.ts",
  "projectId": "jiso",
  "candidates": [
    {
      "vulnSlug": "insecure-crypto",
      "lineNumbers": [78],
      "snippet": "async function readMutationRequestBody(request: Request): Promise<unknown> {\n  const contentType = request.headers.get('content-type')?.toLowerCase() ?? '';\n  if (contentType.includes('application/json')) return request.json();\n  return request.formData();\n}",
      "matchedPattern": "weak cipher algorithm"
    }
  ],
  "lastScannedAt": "2026-06-14T05:38:42.407Z",
  "lastScannedRunId": "20260614053841-358cb3413e9326e8",
  "fileHash": "6230db2a4d4c66bbab375bad2cffa38aabddad8822784bd53e087791cd700f0c",
  "findings": [
    {
      "severity": "MEDIUM",
      "vulnSlug": "open-redirect",
      "title": "Default mutation redirect derives the 303 Location from an unvalidated Referer header",
      "description": "On the no-JS POST-redirect-GET path, a successful mutation returns a 303 whose `Location` is `endpointRequest.redirectTo`, which `handleAppMutationRequest` defaults to `defaultMutationRedirectTo(mutationRequest, currentUrl)` (L62-63). That helper reads the attacker-influenceable `Referer` request header and: (1) in its catch branch returns the **raw, unvalidated `referer` string** verbatim (L88-89) — if it is a protocol-relative value like `//evil.com` or `/\\evil.com` (which `new URL()` rejects, hitting the catch), it becomes the `Location` of the 303 redirect; and (2) in the happy path returns `appRequestUrl(url)` = `${pathname}${search}${hash}` (app-document.ts:90-92), which does NOT strip a `//host` pathname (`new URL('https://victim.com//evil.com').pathname === '//evil.com'`), so a `//host` value still passes through as a protocol-relative redirect. This is the same-origin redirect guard that the framework already implements correctly elsewhere — better-auth's `redirectPath` rejects any value that does not start with `/` or that starts with `//` (better-auth/src/index.ts:1204-1207) — so this default is inconsistent with the framework's own pattern. Practical exploitability is limited (hence low confidence): the `Location` is only emitted on the no-JS success path, which requires a valid session-bound CSRF token (so the form must be same-origin), and real browsers always send an absolute URL as `Referer`, which normalizes to a same-origin path — so the dangerous catch/`//host` cases are not reachable via an ordinary victim browser flow. It is nonetheless a real latent open-redirect defect: any caller, proxy/referrer-policy rewrite, or future reuse of `defaultMutationRedirectTo` that yields a malformed or `//`-prefixed value produces an attacker-controlled redirect target.",
      "lineNumbers": [62, 63, 82, 83, 86, 89],
      "recommendation": "Do not return the raw `referer`. Reuse the framework's existing same-origin guard (as in better-auth `redirectPath`): require the resolved redirect to be a string that starts with a single '/' and not '//' (and not '/\\'), otherwise fall back to a safe default such as `currentUrl` or '/'. Apply the same check to the `appRequestUrl(url)` result so a `//host` pathname cannot pass through, and drop the catch-branch `return referer;` in favor of the fallback.",
      "confidence": "low",
      "producedByRunId": "20260614053853-41cbf55110df05d3"
    }
  ],
  "analysisHistory": [
    {
      "runId": "20260614053853-41cbf55110df05d3",
      "investigatedAt": "2026-06-14T05:50:29.019Z",
      "durationMs": 81477,
      "durationApiMs": 81871.8,
      "agentType": "claude-agent-sdk",
      "model": "claude-opus-4-8",
      "modelConfig": {
        "model": "claude-opus-4-8"
      },
      "agentSessionId": "46ee94e1-82b1-4d57-bcd5-a82c30253a0a",
      "findingCount": 1,
      "numTurns": 3.6,
      "phase": "process",
      "costUsd": 0.4086361,
      "usage": {
        "inputTokens": 552.4,
        "outputTokens": 6194.2,
        "cacheReadInputTokens": 120604.6,
        "cacheCreationInputTokens": 18990.2
      },
      "refusal": {
        "refused": false,
        "skipped": [],
        "raw": "```json\n{\"refused\": false, \"reason\": null, \"skipped\": []}\n```"
      }
    }
  ],
  "status": "analyzed",
  "gitInfo": {
    "recentCommitters": [
      {
        "name": "Will Chen",
        "email": "7344640+wwwillchen@users.noreply.github.com",
        "date": "2026-06-13T02:40:15-07:00"
      }
    ],
    "enrichedAt": "2026-06-14T05:50:29.046Z"
  }
}
