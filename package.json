{
  "name": "kovo-monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*",
    "conformance/*",
    "site"
  ],
  "type": "module",
  "scripts": {
    "acceptance": "pnpm run check && pnpm run check:api-surface && pnpm run test && pnpm run test:browser && pnpm run check:build && pnpm run check:publish && pnpm run test:p10-perf && pnpm run test:conformance && pnpm run check:kovo",
    "check": "pnpm run check:inline-loader && vp check && vp run typecheck-examples",
    "check:api-surface": "node scripts/api-surface-gate.mjs",
    "check:build": "pnpm run check:inline-loader && vp run build",
    "check:publish": "node scripts/build-publish.mjs",
    "check:kovo": "vp run kovo-check",
    "check:inline-loader": "pnpm --filter @kovojs/runtime run check:inline-loader",
    "docs:serve": "pnpm run check:build && pnpm --filter @kovojs/site run build && HOST=0.0.0.0 node site/scripts/serve.mjs",
    "test": "vitest --run",
    "test:browser": "vp run browser",
    "test:conformance": "vp run conformance",
    "test:p10-perf": "vp run p10-perf"
  },
  "devDependencies": {
    "@types/node": "25.9.2",
    "@typescript/native-preview": "7.0.0-dev.20260610.1",
    "@vitest/browser-playwright": "4.1.8",
    "playwright": "^1.60.0",
    "typescript": "^6.0.3",
    "vite-plus": "0.1.24",
    "vitest": "4.1.8"
  },
  "packageManager": "pnpm@10.12.1"
}
