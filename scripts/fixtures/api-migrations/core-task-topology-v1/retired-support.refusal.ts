// @ts-nocheck -- migration refusal intentionally imports a retired named support contract.
import type { HmacSignatureOptions } from '@kovojs/core/webhooks';

export type AppWebhookOptions = HmacSignatureOptions;
