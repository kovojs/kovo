interface:
  display_name: 'Implement Plan'
  short_description: 'Integrate large worker chunks for a Kovo plan'
  default_prompt: 'Use $implement-plan to complete plans/example.md by keeping the main agent on integration, assigning large disjoint worker chunks, verifying batches, merging to local main before pushing, monitoring CI, and summarizing the result.'
