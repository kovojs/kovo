interface:
  display_name: 'Implement Plan'
  short_description: 'Integrate worker slices for a Kovo plan'
  default_prompt: 'Use $implement-plan to complete plans/example.md by keeping the main agent on integration, assigning disjoint worker slices, verifying batches, pushing to main, monitoring CI, and summarizing the result.'
