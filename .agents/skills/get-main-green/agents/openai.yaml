interface:
  display_name: 'Get Main Green'
  short_description: 'Fix failing main GitHub Actions checks'
  default_prompt: 'Use $get-main-green to inspect the latest main commit, start local repair from actionable logs, wait for all latest-commit CI runs to finish before pushing, and monitor the follow-up run.'
