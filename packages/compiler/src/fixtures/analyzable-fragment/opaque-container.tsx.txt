import { endpoint } from '@kovojs/server';

export const root = endpoint('/ledger/container', {
  handler(_input, context) {
    const box = { database: context.db };
    void box;
    return Response.json({ ok: true });
  },
});
