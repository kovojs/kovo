import { endpoint } from '@kovojs/server';

export const root = endpoint('/ledger/nested', {
  handler(_input, context) {
    const deferred = () => context.db.select();
    void deferred;
    return Response.json({ ok: true });
  },
});
